module Lib where


-- when post job, 
--it need to draft headhunt smartcontract
draftHeadHuntContract = undefined

-- after a candidate apply a job, 
--then automatically  generate a Headhunt Contract
generateHeadhuntContract = undefined

-- after every  updateRecruitState (updateRecruitState)
-- it need to validate HeadHunt contract and then update contract status 
-- and executive contract if need
validateContract = undefined
-- it also need to validate the contract every date too

---------------------------
-- incase of having dispute need handle it

---------------------------
-- job status update every day
updateJobStatus = undefined


-----
showJob = undefined

--
showApplication = undefined

--
showHHcontract = undefined


